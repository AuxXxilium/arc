# https://taskfile.dev

version: "3"

tasks:
  build-img:
    cmds:
      - git pull
      - ./img-gen.sh

  build-img-s:
    cmds:
      - git pull
      - ./img-gen-s.sh

  compile-kpatch:
    dir: kpatch
    cmds:
      - make clean all
      - mv kpatch ../files/board/arc/overlayfs/opt/arc/