# https://taskfile.dev

version: "3"

tasks:
  build-evo:
    cmds:
      - git reset --hard
      - git pull
      - ./img-gen.sh evo
  build-essential:
    cmds:
      - git reset --hard
      - git pull
      - ./img-gen.sh essential