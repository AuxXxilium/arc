# https://taskfile.dev

version: '3'

tasks:
  build-img:
    cmds:
      - git reset --hard
      - git pull
      - ./img-gen.sh

  compile-kpatch:
    dir: kpatch
    cmds:
      - make clean all
      - mv kpatch ../files/board/arpl/overlayfs/opt/arpl/